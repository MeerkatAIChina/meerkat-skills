---
skill_id: "ui-design"
name: "UI 设计"
version: "1.0.0"
status: "testing"

owner: "LinkmasterLing"
risk_level: "medium"
created_at: "2026-05-18"
updated_at: "2026-05-19"

description: >
  UI界面设计与组件规范。将用户需求转化为可执行的UI设计方案，输出设计规范与组件定义。

category: "design"
type: "prompt"

trigger:
  keywords:
    - "UI设计"
    - "界面设计"
    - "组件规范"
    - "设计系统"
    - "Figma"
  intent:
    - "ui_design"
    - "interface_design"
    - "component_spec"
  min_confidence: 0.75

input_schema:
  required:
    - "task_description"
  optional:
    - "context"
    - "constraints"

output_schema:
  type: "markdown"
  additional_files:
    - "checklist.md"

dependencies:
  models:
    - "kimi-coding/k2.6"

eval:
  suite_id: "ui-design-eval-v1"
  min_score: 80
  format_pass_rate: 0.95
  regression_pass_rate: 0.90

release:
  strategy: "canary"
  canary_steps: [10, 50, 100]
  rollback_version: "1.0.0"

changelog: "changelog.md"
